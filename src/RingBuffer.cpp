#include "RingBuffer.hpp"

// In a template-heavy project, specific non-template 
// logic or logging utilities would go here.
// Currently, all RingBuffer logic resides in the header 
// to allow the compiler to instantiate types.